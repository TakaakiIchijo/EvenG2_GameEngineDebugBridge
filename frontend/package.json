{
  "name": "even-g2-debug-bridge-frontend",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Engine-agnostic Even G2 debug log viewer",
  "engines": {
    "node": "^20.0.0 || >=22.0.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "generate:manifest": "node scripts/generate-app-manifest.mjs",
    "pack": "npm run build && npm run generate:manifest && evenhub pack app.json dist -o ../release/even-g2-debug-bridge.ehpk"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.13"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
