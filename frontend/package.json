{
  "name": "even-g2-debug-bridge-frontend",
  "version": "1.0.0",
  "type": "module",
  "description": "Even G2 Debug Bridge - Frontend for displaying engine logs on smart glasses",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.7",
    "@evenrealities/evenhub-simulator": "^0.4.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
